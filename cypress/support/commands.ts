/// <reference types="cypress" />

// Shared custom commands for e2e and component tests.
// Example:
// Cypress.Commands.add("getByTestId", (testId: string) => {
//   return cy.get(`[data-cy="${testId}"]`);
// });
